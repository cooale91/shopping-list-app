# Shopping List App v4

New streamlined shopping features:
- bulk shopping text export for supermarket shopping-list pages
- preferred product mapping, so generic ingredients become specific searches
- top quick links section for the first items you want to open fastest

## Run
```bash
python -m pip install -r requirements.txt
python -m streamlit run app.py
```
